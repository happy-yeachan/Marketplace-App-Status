name: CI

# Typecheck + unit tests + production build on every push/PR to main.
# The unit tests cover the status-payload parsers (src/lib/status-extract.ts,
# with real vendor payload fixtures), the share-link codec, and the SSRF guard
# — the three modules where regressions are silent and user-facing.
#
# Note: `npm run lint` is NOT part of CI yet — the react-hooks plugin flags
# several pre-existing patterns (ref-mutation-in-effect etc.) that need a
# dedicated cleanup pass first.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Unit tests
        run: npm test

      - name: Production build
        run: npm run build
